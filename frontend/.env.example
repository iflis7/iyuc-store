# Medusa backend (browser must reach this; add origin to backend STORE_CORS)
VITE_MEDUSA_BACKEND_URL=http://localhost:9000
# Publishable API key from Medusa Admin (Settings → Developer → Publishable API Keys)
VITE_MEDUSA_PUBLISHABLE_KEY=pk_test
# Set to "true" to use mock data instead of the backend
# VITE_USE_MOCK=true
